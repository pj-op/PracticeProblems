package com.annotation.test;

import java.io.FileNotFoundException;

public class TestingAnnotation {

    public static void main(String[] args) throws IllegalAccessException, FileNotFoundException {

        /*Square square = new Square();
        square.setWidth(11);

        for (Field field : square.getClass().getFields()) {
            System.out.print("Fields: " + field);
            if (field.isAnnotationPresent(LengthCheck.class)) {
                if (field.get(square) instanceof Integer a) {
                    if (a > 10) {
                        throw new RuntimeException("Default 4 or less than 10 allowed");
                    }
                }
            }
        }*/

    }
}
